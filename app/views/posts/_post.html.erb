<div id="<%= dom_id post %>" class="post">
  <h3 class="post__title"><%= post.name %></h3>
  <p class="post__body"><%= post.body %></p>

  <% if user_signed_in? %>
    <p class="post__author">Por <%= post.user.name %> <%= post.user.surname %></p>
  <% end %>
</div>
